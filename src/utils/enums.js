const integracaoStatus = Object.freeze({
    EXCLUIDO: 0,
    ATIVO: 1,
    PENDENTE: 2,
    INATIVO: 3
});

const marketplaces = Object.freeze({
    MERCADO_LIVRE: 1,
});

module.exports = { integracaoStatus, marketplaces };